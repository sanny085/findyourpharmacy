import { PHARMACY_LIST } from './actionType.js';

const initialState = {
    pharmacy: [],
    store: [],
    storeDetails: []
};

export const storeReducer = (initialState, action) => {
    switch (action) {
        case PHARMACY_LIST:
    }
};
